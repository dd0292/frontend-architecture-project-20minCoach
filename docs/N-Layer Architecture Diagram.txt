---
config:
  layout: elk
---
flowchart TB
  subgraph Presentation["Presentation Layer"]
    UI["src/App.tsx<br>src/screens/<br>src/components/"]
  end
  subgraph Controllers["Controllers Layer"]
    TRL["src/controllers/*"]
  end
  subgraph State["State Management"]
    STATE["src/state/store.ts<br>src/slices/*"]
  end
  subgraph Business["Business Layer"]
    BUS["src/business/services/* (coachService.ts)"]
  end
  subgraph Api["API / Proxy Layer"]
    API["src/api/client.ts<br>Auth Provider (Auth0)"]
  end
  subgraph Persistence["Persistence / Backend"]
    DB["Backend API / Database<br>(EXAMPLE)"]
  end
    Presentation --> Controllers
    Controllers --> State & Business
    Business --> Api
    Api --> Persistence
    State --> Business